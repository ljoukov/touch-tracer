{
	"name": "touch-tracer-sv-clean-bkzljc",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "vite dev",
		"build": "vite build && npm run build:dist",
		"build:dist": "rm -rf dist && mkdir -p dist && cp -R .svelte-kit/output/client/. dist/ && cp -R .svelte-kit/output/prerendered/pages/. dist/ && if [ -d .svelte-kit/output/prerendered/dependencies ]; then cp -R .svelte-kit/output/prerendered/dependencies/. dist/; fi",
		"preview": "vite preview",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^7.0.0",
		"@sveltejs/kit": "^2.50.2",
		"@sveltejs/vite-plugin-svelte": "^6.2.4",
		"svelte": "^5.51.0",
		"svelte-check": "^4.3.6",
		"typescript": "^5.9.3",
		"vite": "^7.3.1"
	}
}
